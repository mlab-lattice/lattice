[Unit]
# Often times mounting the kube-etcd volume (/dev/xvdf) took longer than the default
# 90s timeout for dev-xvdf.device. Add this to extend the amount of time that systemd
# waits for the device to be attached.
JobRunningTimeoutSec=5m
