[Unit]
# Wait until etcd is up and dns is registered to start.
Requires=kube-etcd.service register-master-node-dns.service
After=kube-etcd.service register-master-node-dns.service

[Service]
Environment="KUBELET_OPTS=--pod-manifest-path=/var/opt/kubernetes/manifests"
