[Unit]
# If the device is attached no need to try to attach it
ConditionPathExists=!/dev/xvdf

# Wait until the network is up to try to attach the disk.
Requires=network-online.target
After=network-online.target

[Service]
Type=oneshot
ExecStart=/opt/lattice/bin/master-attach-etcd-volume /dev/xvdf
