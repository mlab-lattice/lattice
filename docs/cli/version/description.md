Shows the current installed version of the CLI and the most recent version. You can update on Homebrew by running:

```
$ brew upgrade mlab-lattice/lattice/lattice-cli
```

Or by reinstalling the binary.
